import React from 'react';
import {Text, StyleSheet, Platform} from 'react-native';
import {styles} from './styles';

const Form = () => {
  return <Text style={styles.header}>CryptoQuote</Text>;
};

export default Form;
